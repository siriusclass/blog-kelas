---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Container from '@/components/Container.astro'
import Layout from '@/layouts/Layout.astro'
---

<Layout title="Chatroom" description="Ngobrol bareng pengunjung blog">
  <Container class="flex flex-col gap-y-8">
    <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-slate-50 via-white to-slate-100 dark:from-black dark:via-neutral-900 dark:to-black p-8 shadow-sm border border-slate-200/50 dark:border-gray-800/50">
      <div class="relative z-10 flex flex-col gap-y-8">
        
        <!-- Breadcrumbs -->
        <Breadcrumbs
          items={[
            { label: 'Home', href: '/', icon: 'lucide:home' },
            { label: 'Chatroom', icon: 'lucide:messages-square' },
          ]}
        />

        <!-- Chatbox -->
        <div class="flex flex-col gap-y-4 min-h-[calc(100vh-18rem)]">
          <h2 class="text-xl font-semibold">Chatroom Pengunjung 🚀</h2>
          <p class="text-muted-foreground text-sm">
            Tempat ngobrol santai bareng pengunjung lain 👇
          </p>

          <!-- Responsive iframe -->
          <div class="relative w-full flex-1">
            <iframe 
              src="https://www6.cbox.ws/box/?boxid=844227&boxtag=aO8eGU"
              class="absolute inset-0 w-full h-full border rounded-md"
              allowtransparency="yes"
              allow="autoplay"
              frameborder="0"
              marginheight="0"
              marginwidth="0"
              scrolling="auto"
            >
            </iframe>
          </div>
        </div>

      </div>
    </div>
  </Container>
</Layout>

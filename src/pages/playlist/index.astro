---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Container from '@/components/Container.astro'
import Layout from '@/layouts/Layout.astro'
---

<Layout title="Spotify Playlist" description="Dengerin playlist favorit langsung di blog 🎵">
  <Container class="flex flex-col gap-y-8">
    <div class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-slate-50 via-white to-slate-100 dark:from-black dark:via-neutral-900 dark:to-black p-8 shadow-sm border border-slate-200/50 dark:border-gray-800/50">
      <div class="relative z-10 flex flex-col gap-y-8">
        
        <!-- Breadcrumbs -->
        <Breadcrumbs
          items={[
            { label: 'Home', href: '/', icon: 'lucide:home' },
            { label: 'Spotify', icon: 'lucide:music' },
          ]}
        />

        <!-- Spotify Player -->
        <div class="flex flex-col gap-y-4 min-h-[calc(100vh-18rem)]">
          <h2 class="text-xl font-semibold">Playlist Favorit 🎶</h2>
          <p class="text-muted-foreground text-sm">
            Putar langsung playlist dari Spotify 👇
          </p>

          <!-- Responsive iframe -->
          <div class="relative w-full max-w-2xl">
            <iframe
              style="border-radius:12px"
              src="https://open.spotify.com/embed/playlist/23jXWFCiW82PZgIQwJaeIS?utm_source=generator"
              width="100%"
              height="352"
              frameborder="0"
              allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
              loading="lazy"
            ></iframe>
          </div>
        </div>

      </div>
    </div>
  </Container>
</Layout>
